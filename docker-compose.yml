version: "3.8"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=process.env.MONGO_URL
      - JWT_SECRET=process.env.JWT_SECRET
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    ports:
      - "27017:27017"